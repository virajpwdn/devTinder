apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: devtinder
  labels:
    app: frontend-devtinder-svc
  
spec:
  selector:
    app: frontend-devtinder-svc
  ports:
  - name: nginx
    port: 80
    targetPort: 80
    protocol: TCP
  type: ClusterIP
